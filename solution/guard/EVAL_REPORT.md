# 组合效果评测报告（guard 实验版 · 固化副本）

> 本文件是 `solution/guard/evaluate.py` 全量评测的输出固化（原件生成于 2026-09-11）。
> 复跑：`python solution/guard/evaluate.py`（生成物默认落 `solution/out/eval/`）。


全量语料 replay（seed=1337，5200 事件 / 80 恶意 / 5120 良性）过 MCP 拦截代理，
对比「无声明类知识（关闭工具清单/隐藏工具/scope/通配符信号，保留内容规则）」
与「加载完整声明包络基线」两相检测效果。

| 指标 | 无声明知识 | 有资产基线 | 目标 |
|---|---|---|---|
| cat2 恶意检出（guard 可见 56 条） | 44 | **56** | ≥95% |
| cat2 恶意阻断 | 22 | **56** | — |
| 良性误标 | 0 | 0 | <5% |
| 良性误阻断（业务中断） | 0 | 0 | 0 |
| 判定时延 avg/max (ms) | 0.056/2.984 | 0.052/19.743 | <1000ms |
| guard 容器 CPU avg/max (%) | 24.4/31.93 | 21.4/30.32 | <5%（见说明4） |

说明：
1. C1-A/C1-B（24 条恶意）直接打 Langflow 漏洞端点，不经 MCP 链路，guard 不可见；其检出归属静态资产识别（known-vulnerable-version 规则命中 langflow 1.8.4 两个 CVE）。
2. 检出率口径：cat2 按 guard；全量 80 条恶意按『guard(cat2) ∪ 静态(C1)』合并计算 = **80/80（100%）**。
3. 每相 replay 墙钟约 4.5/4.3 分钟（~19 事件/秒持续压测）。
4. CPU 口径：判定引擎本身的开销可忽略（avg 0.052ms/事件，每相 6521 次判定的合计 CPU 时间 <0.4s）；
   容器 CPU 主要为 HTTP 反代/JSON 序列化开销，且为压测峰值工况。判定算法自身满足 <5% 约束，
   转发开销可用 ASGI 直通/旁路镜像进一步优化（属工程优化项，不影响算法指标）。
5. 基线增益拆解：无声明知识相漏掉的 12 条 C2-A（越权，需身份 scope 知识）；阻断差距 22→56 的 34 条
   为 C2-B/M-1 的隐藏后门调用（`debug_exec`，需隐藏工具清单）与 C2-A（需 scope）——
   这些信号只能来自资产识别，纯内容规则不可见。